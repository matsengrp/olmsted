# Python dependencies for bin/aws_*.py and bin/check_invalidation_status.py.
# Keep this file minimal — only direct imports of the bin/ scripts belong here.
# Everything else was stale pre-modernization noise.
boto3==1.26.137
pyyaml==6.0
