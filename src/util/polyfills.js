/**
 * Polyfills module — intentionally empty.
 *
 * All polyfills (Promise, Object.assign, fetch, String.startsWith) were
 * removed in the dependency cleanup (2026-03-25) because they are natively
 * supported by all browsers we target. This file is kept as an empty import
 * so existing import statements don't break.
 */
